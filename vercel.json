{
  "version": 2,
  "installCommand": "npm install && npm --prefix next install",
  "buildCommand": "npm run build",
  "outputDirectory": "next/.next",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "next/$1"
    }
  ]
}
