registry=https://registry.npmjs.org/
# Use an auth token when registry access requires authentication (avoids 403s in restricted environments)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
always-auth=true
